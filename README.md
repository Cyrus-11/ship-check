# ship-check
Shipcheck is a local-first release-readiness CLI for Node.js and TypeScript repositories. Shipcheck inspects the repository, runs the project build and test commands, validates required environment-variable names, and converts the results into a consistent release score and status
